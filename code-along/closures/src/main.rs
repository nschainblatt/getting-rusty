use closures;

fn main() {
    closures::immutable_borrow_closure();
    closures::mutable_borrow_closure();
    closures::moving_ownership_to_another_thread_closure();
    closures::sort_rect_by_width();
}
